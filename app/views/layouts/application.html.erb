<!DOCTYPE html>
<html>
  <head>
    <style type="text/css">
      html, body {
        margin: 0;
        padding: 0;
        height: 100%;
        width: 100%;
      }
      body {
        overflow: auto;
        position: relative;
      }
      body:before {
        content: "";
        position: fixed;
        left: -5%;
        right: -5%;
        z-index: -1;

        display: block;
        background-image: url('/bg.jpg');
        background-size:cover;
        width: 110%;
        height: 110%;

        -webkit-filter: blur(3px);
        -moz-filter: blur(3px);
        -o-filter: blur(3px);
        -ms-filter: blur(3px);
      }
    </style>

    <title>TURD Mate</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
